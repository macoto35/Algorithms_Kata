#include <iostream>
#include "Node.h"

using namespace std;

void main()
{
	// Node* b = new Node(2);
	Node* a = new Node(1, NULL);

	//cout << a->data << a->next->data << endl;
	cout << a=>data << endl;
}
